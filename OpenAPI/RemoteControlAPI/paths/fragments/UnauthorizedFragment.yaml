# vmp-http-spec - OpenAPI Specification for the vmp-server HTTP interface
# Copyright (C) 2023 Hugo Melder
# 
# SPDX-License-Identifier: MIT
      
description: > 
  The user is not authenticated.
  Possible error codes:
    - 10001: Invalid JWT token
    - 10002: JWT token expired
content:
  application/json:
    schema:
      $ref: '../../components/schemas/GenericError.yaml'
    examples:
      Default:
        value:
          $ref: '../../components/examples/DefaultUnauthorizedResponse.yaml'